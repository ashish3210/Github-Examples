name: "workflow commands"

on: ['push']

jobs:
  my-job:
    runs-on: ubuntu-latest
     steps:
      - name: "group log"
        run: |
          echo "group mesg"
          echo "middle"
          echo "end mesg"